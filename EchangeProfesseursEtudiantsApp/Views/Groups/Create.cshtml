@model EchangeProfesseursEtudiantsApp.ViewModel.GroupViewModel

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Group</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Groups.FirstOrDefault().Namegroup" class="control-label"></label>
                <input asp-for="Groups.FirstOrDefault().Namegroup" class="form-control" />
                @*<span asp-validation-for="Groups.FirstOrDefault().Name" class="text-danger"></span>*@
            </div>

            <div class="form-group">
                <label asp-for="Groups.FirstOrDefault().Descriptiongroup" class="control-label"></label>
                <input asp-for="Groups.FirstOrDefault().Descriptiongroup" class="form-control" />
                @*<span asp-validation-for="Description" class="text-danger"></span>*@
            </div>

            <div class="form-group">
                <label asp-for="applicationusers.FirstOrDefault().Email" class="control-label"></label>
                <input asp-for="applicationusers.FirstOrDefault().Email" class="form-control" />
                @*<span asp-validation-for="Description" class="text-danger"></span>*@
            </div>

            

            @*<div class="col-4">@Html.DropDownList("receiver",Model.applicationuser.Email.ToArray(), "All")</div>*@

            @*<div class="form-floating">
                <select asp-for="applicationuser" asp-items="@Model.applicationuser.Firstname.ToString()" class="form-select" aria-required="true">

                    <option disabled selected>Select Role</option>

                </select>
                <span asp-validation-for="" class="text-danger"></span>
            </div>*@

            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
