@*@model EchangeProfesseursEtudiantsApp.Models.Mark*@
@model EchangeProfesseursEtudiantsApp.ViewModel.MarkViewModel

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Mark</h4>
<hr />
<div class="row">
    <div class="col-md-4">

        <form asp-action="Create">
            <input type="hidden" asp-for="elements.FirstOrDefault().Idelement" />
            <input type="hidden" asp-for="applicationusers.FirstOrDefault().Email" />
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="applicationusers.FirstOrDefault().Firstname" class="control-label"></label>
                <input disabled asp-for="applicationusers.FirstOrDefault().Firstname" class="form-control" />
                @*<span asp-validation-for="Name" class="text-danger"></span>*@
            </div>
            <div class="form-group">
                <label asp-for="applicationusers.FirstOrDefault().Lastname" class="control-label"></label>
                <input disabled asp-for="applicationusers.FirstOrDefault().Lastname" class="form-control" />
                @*<span asp-validation-for="Description" class="text-danger"></span>*@
            </div>
            <div class="form-group">
                <label asp-for="marks.FirstOrDefault().themark" class="control-label"></label>
                <input asp-for="marks.FirstOrDefault().themark" class="form-control" />
                @*<span asp-validation-for="Coefficient" class="text-danger"></span>*@
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>


    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
